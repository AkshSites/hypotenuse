// Set this to a real URL to turn the coffee button into an outbound link.
// Leave it null to keep it as an on-brand placeholder — no styling changes
// needed either way, CoffeeButton.astro handles both states.
export const COFFEE_URL: string | null = null;
